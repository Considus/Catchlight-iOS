# Xcode build output
.build/
build/
DerivedData/

# XcodeGen — project is generated from project.yml, not checked in
*.xcodeproj/
Catchlight.xcodeproj/

# Xcode user data
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside

# Swift Package Manager
.swiftpm/
.build/
Packages/
Package.resolved

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Instruments / Crashlytics / other tools
*.dSYM/
*.dSYM.zip

# Fastlane (future use)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output/

# Environment / secrets — belt and braces
*.env
.env*
secrets/
*.p8
*.p12
*.mobileprovision
AuthKey_*.p8

# Claude Code working files
.claude/

# 7.12 OS-floor verification harness — TEMPORARY, manual-only (run with
# -only-testing: on an ERASED simulator; drives the Settings app for AX5).
# Deliberately NOT committed: project.yml globs Tests/CatchlightUITests so it
# still compiles for local 7.12 runs, but it must not enter the public repo or
# the CI `xcodebuild test` scheme. Delete file + this rule after the 7.12 pass
# is signed off (Decision D-024).
Tests/CatchlightUITests/OSFloorTourUITests.swift
